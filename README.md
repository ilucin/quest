# q — Quest orchestrator for Claude Code agents

Rust CLI + TUI that organizes Claude Code agent sessions into **Quests**
(master + workers in tmux, hooks as event source, SQLite per machine,
regenerable brief).

Status: M0 skeleton in progress.
